# 自动化内容生成与发布闭环使用指南

## 概述

本项目实现了完整的内容生成到发布的自动化闭环：

1. **内容自动生成** - 从热点新闻抓取、搜索参考资料到生成文章
2. **自动批量发布** - 双层循环将所有文章发布到所有平台

### 发布逻辑

采用**双层循环**的发布策略：

```
外层循环：遍历每篇文章
└── 内层循环：遍历每个平台
    └── 发布当前文章到当前平台
```

**支持的平台**（6个）：
- CSDN
- 掘金（Juejin）
- 知乎（Zhihu）
- 51CTO
- 阿里云（Alicloud）
- 今日头条（Toutiao）

## 快速开始

### 1. 环境准备

```bash
# 设置智谱AI API密钥
export ZHIPUAI_API_KEY='your-api-key-here'

# 启动 Chrome 调试模式
bash scripts/start_chrome.sh
```

### 2. 配置平台开关

编辑 `config/common.yaml`，启用需要发布的平台：

```yaml
enable:
  csdn: true
  juejin: true
  zhihu: true
  cto51: true
  alicloud: true
  toutiao: true
```

### 3. 运行完整流水线

#### 方式1：使用便捷脚本（推荐）

```bash
# 自动生成内容并发布
bash scripts/auto_publish.sh
```

#### 方式2：使用 Python 脚本

```bash
# 完整流水线：生成 + 发布
python3 auto_publish_pipeline.py \
    --news-limit 10 \
    --article-limit 1 \
    --search-depth quick \
    --delay 2.0 \
    --publish-delay 3.0
```

### 4. 仅发布现有文章

如果已有文章，只想批量发布：

```bash
# 方式1：使用脚本
bash scripts/publish_existing.sh

# 方式2：使用 Python
python3 auto_publish_pipeline.py --skip-generation
```

## 命令行参数详解

### auto_publish_pipeline.py

```bash
python3 auto_publish_pipeline.py [OPTIONS]
```

**参数说明**：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--news-limit` | int | 10 | 抓取的新闻数量 |
| `--article-limit` | int | 1 | 生成的文章数量 |
| `--search-depth` | quick/deep | quick | 搜索深度（quick=快速，deep=深度） |
| `--delay` | float | 2.0 | 内容生成时的API请求间隔（秒） |
| `--publish-delay` | float | 3.0 | 发布时的延迟（秒） |
| `--skip-generation` | flag | false | 跳过内容生成，直接发布已有文章 |
| `--api-key` | string | - | 智谱AI API密钥（可通过环境变量设置） |

**示例**：

```bash
# 生成3篇文章并发布
python3 auto_publish_pipeline.py --article-limit 3

# 深度搜索模式
python3 auto_publish_pipeline.py --search-depth deep

# 只发布现有文章
python3 auto_publish_pipeline.py --skip-generation

# 调整延迟
python3 auto_publish_pipeline.py --delay 3.0 --publish-delay 5.0
```

## 工作流程详解

### 完整流水线流程

```
┌─────────────────────────────────────────────────────────┐
│  阶段 1: 内容生成                                        │
├─────────────────────────────────────────────────────────┤
│  1. 抓取量子位热点新闻（news-limit条）                    │
│  2. 搜索参考资料（search-depth模式）                      │
│  3. 生成文章（article-limit篇）                          │
│  4. 保存到 posts/ 目录                                   │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  阶段 2: 初始化浏览器                                     │
├─────────────────────────────────────────────────────────┤
│  1. 连接 Chrome 调试模式（端口9222）                      │
│  2. 初始化会话管理器                                      │
│  3. 复用浏览器驱动                                        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  阶段 3: 批量发布（双层循环）                             │
├─────────────────────────────────────────────────────────┤
│  外层循环：for 文章 in 文章列表:                          │
│    内层循环：for 平台 in 平台列表:                        │
│      1. 获取平台发布器                                    │
│      2. 设置浏览器驱动                                    │
│      3. 执行发布                                         │
│      4. 记录结果                                         │
│      5. 延迟（publish-delay秒）                          │
│                                                          │
│  输出最终报告                                             │
└─────────────────────────────────────────────────────────┘
```

### 发布顺序示例

假设有2篇文章，启用了3个平台（CSDN、掘金、知乎）：

```
1. 文章1 → CSDN
2. 文章1 → 掘金
3. 文章1 → 知乎
   ↓（文章1完成）
4. 文章2 → CSDN
5. 文章2 → 掘金
6. 文章2 → 知乎
   ↓（文章2完成）
完成所有发布
```

## 输出和日志

### 1. 生成的文章

存放在 `posts/` 目录：

```
posts/
├── AI革新启航-2025年工业智能新纪元.md
├── Kimi-K2引擎-推理AI的突破性进展.md
└── ...
```

### 2. 中间数据

存放在 `data/generated/` 目录：

```
data/generated/
├── 01_crawled_news.json        # 抓取的新闻
├── 02_search_references.json   # 搜索的参考资料
├── 03_generated_articles.json  # 生成的文章信息
└── 04_pipeline_report.txt      # 生成报告
```

### 3. 日志文件

存放在 `data/logs/` 目录：

```
data/logs/
├── auto_publish_pipeline_YYYYMMDD.log
├── csdn_publisher_YYYYMMDD.log
├── juejin_publisher_YYYYMMDD.log
└── ...
```

### 4. 发布报告

流水线结束时会打印详细报告：

```
================================================================================
📊 批量发布完成 - 总体报告
================================================================================
总发布任务: 6
成功: 5
失败: 1
成功率: 83.3%

--------------------------------------------------------------------------------
📋 详细发布记录:
--------------------------------------------------------------------------------

📄 AI革新启航-2025年工业智能新纪元.md
   ✅ CSDN
   ✅ JUEJIN
   ✅ ZHIHU

📄 Kimi-K2引擎-推理AI的突破性进展.md
   ✅ CSDN
   ❌ JUEJIN
   ✅ ZHIHU

================================================================================
```

## 故障排查

### 1. Chrome 连接失败

**错误信息**：
```
❌ 无法连接到 Chrome 调试模式
```

**解决方案**：

```bash
# 检查 Chrome 是否运行
lsof -i :9222

# 重启 Chrome
bash scripts/start_chrome.sh
```

### 2. API Key 未设置

**错误信息**：
```
❌ 请提供智谱AI API Key或设置环境变量 ZHIPUAI_API_KEY
```

**解决方案**：

```bash
# 设置环境变量
export ZHIPUAI_API_KEY='your-api-key-here'

# 或者在命令行中指定
python3 auto_publish_pipeline.py --api-key 'your-api-key-here'
```

### 3. 没有启用的平台

**错误信息**：
```
❌ 没有启用任何发布平台，请检查配置文件
```

**解决方案**：

编辑 `config/common.yaml`，启用至少一个平台：

```yaml
enable:
  csdn: true  # 改为 true
```

### 4. 文章目录不存在

**错误信息**：
```
❌ posts 目录不存在，无文章可发布
```

**解决方案**：

```bash
# 先生成内容
python3 auto_publish_pipeline.py --article-limit 1

# 或者创建目录并手动添加文章
mkdir -p posts
```

## 高级用法

### 1. 自定义发布策略

如果需要更复杂的发布策略，可以修改 `auto_publish_pipeline.py`：

```python
# 例如：优先级发布
priority_platforms = ['csdn', 'juejin']
other_platforms = ['zhihu', 'cto51', 'alicloud', 'toutiao']

# 先发布到优先平台
for article in articles:
    for platform in priority_platforms:
        publish_article_to_platform(article, platform)
    
    # 等待一段时间
    time.sleep(60)
    
    # 再发布到其他平台
    for platform in other_platforms:
        publish_article_to_platform(article, platform)
```

### 2. 定时自动运行

使用 cron 定时执行：

```bash
# 编辑 crontab
crontab -e

# 添加定时任务（每天上午10点运行）
0 10 * * * cd /path/to/posts-copilot && bash scripts/auto_publish.sh >> logs/cron.log 2>&1
```

### 3. 批量处理历史文章

如果有大量历史文章需要发布：

```bash
# 发布所有现有文章
python3 auto_publish_pipeline.py --skip-generation --publish-delay 5.0
```

## 最佳实践

### 1. 延迟设置

- **内容生成延迟**（`--delay`）：建议 2-3 秒，避免 API 限流
- **发布延迟**（`--publish-delay`）：建议 3-5 秒，避免平台反爬

### 2. 批量发布

- 首次使用建议生成 1 篇文章测试
- 确认无误后再批量生成和发布
- 大批量发布建议在凌晨进行，避免高峰期

### 3. 错误处理

- 脚本会自动记录所有错误日志
- 发布失败的文章可以稍后重试
- 建议定期检查日志文件

### 4. 内容质量

- 使用 `--search-depth deep` 提高文章质量
- 生成后建议人工审核内容
- 可以手动编辑 `posts/` 目录下的文章

## 文件结构

```
posts-copilot/
├── auto_publish_pipeline.py    # 主流水线脚本
├── publish.py                   # 原始发布脚本（交互式）
├── scripts/
│   ├── auto_publish.sh         # 自动发布脚本
│   ├── publish_existing.sh     # 发布现有文章脚本
│   └── start_chrome.sh         # Chrome 启动脚本
├── generate/
│   ├── auto_content_pipeline.py    # 内容生成流水线
│   ├── qbitai_crawler.py           # 新闻爬虫
│   ├── reference_searcher.py       # 参考资料搜索
│   └── enhanced_content_generator.py  # 文章生成器
├── src/
│   └── publisher/              # 各平台发布器
│       ├── csdn_publisher.py
│       ├── juejin_publisher.py
│       ├── zhihu_publisher.py
│       ├── cto51_publisher.py
│       ├── alicloud_publisher.py
│       └── toutiao_publisher.py
├── posts/                      # 生成的文章
├── data/
│   ├── generated/             # 中间数据
│   └── logs/                  # 日志文件
└── config/                    # 配置文件
    └── common.yaml
```

## 总结

通过 `auto_publish_pipeline.py`，你可以：

✅ **一键生成内容** - 从热点新闻到完整文章  
✅ **自动批量发布** - 所有文章发布到所有平台  
✅ **双层循环逻辑** - 保证所有内容都能发布到所有平台  
✅ **完整的日志记录** - 方便追踪和调试  
✅ **灵活的配置** - 支持多种运行模式  

现在，你可以真正实现内容创作和分发的自动化闭环！🚀
