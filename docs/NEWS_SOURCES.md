# 多新闻源配置指南

## 概述

posts-copilot 现已支持多个新闻源，可以从不同的 AI 资讯平台抓取最新新闻作为内容生成的素材。

## 支持的新闻源

| 新闻源 | 说明 | 爬虫文件 |
|--------|------|----------|
| **aibase** | AIBase.com - AI 综合资讯平台（**默认**） | `generate/aibase_crawler.py` |
| **qbitai** | 量子位 - 专业 AI 媒体 | `generate/qbitai_crawler.py` |

---

## 配置方式

### 方式一：配置文件（推荐）

编辑 `config/common.yaml`：

```yaml
# 新闻源配置（内容生成）
# 支持多个新闻源，用逗号分隔
# 可选值: aibase, qbitai
# 默认使用 aibase
news_sources: aibase

# 示例：使用多个新闻源
# news_sources: aibase,qbitai
```

**单个新闻源示例**：
```yaml
# 只使用 AIBase
news_sources: aibase

# 只使用量子位
news_sources: qbitai
```

**多个新闻源示例**：
```yaml
# 同时使用 AIBase 和量子位
news_sources: aibase,qbitai
```

### 方式二：命令行参数

运行时通过 `--news-sources` 参数指定：

```bash
# 使用 AIBase
python generate/auto_content_pipeline.py --news-sources aibase

# 使用量子位
python generate/auto_content_pipeline.py --news-sources qbitai

# 同时使用两个新闻源
python generate/auto_content_pipeline.py --news-sources aibase,qbitai
```

**优先级**：命令行参数 > 配置文件

---

## 使用示例

### 1. 使用默认新闻源（AIBase）

```bash
# 不指定任何参数，自动使用配置文件中的默认值
python generate/auto_content_pipeline.py
```

### 2. 使用量子位新闻源

**方式 A：修改配置文件**
```yaml
# config/common.yaml
news_sources: qbitai
```

```bash
python generate/auto_content_pipeline.py
```

**方式 B：命令行指定**
```bash
python generate/auto_content_pipeline.py --news-sources qbitai
```

### 3. 同时使用多个新闻源

**方式 A：修改配置文件**
```yaml
# config/common.yaml
news_sources: aibase,qbitai
```

```bash
python generate/auto_content_pipeline.py
```

**方式 B：命令行指定**
```bash
python generate/auto_content_pipeline.py --news-sources aibase,qbitai
```

### 4. 完整示例：多源抓取 + 生成文章

```bash
# 从 AIBase 和量子位各抓取 10 条新闻，生成 3 篇文章
python generate/auto_content_pipeline.py \
    --news-sources aibase,qbitai \
    --news-limit 10 \
    --article-limit 3
```

**说明**：
- `--news-limit 10`：每个新闻源抓取 10 条新闻（总计 20 条）
- `--article-limit 3`：从 20 条新闻中选择前 3 条生成文章

---

## 运行输出示例

### 单新闻源模式

```
================================================================================
🚀 自动化内容生成流水线启动
================================================================================
新闻源: aibase
每个源抓取新闻数: 10
生成文章数: 1
搜索深度: quick
输出目录: posts
================================================================================

📡 正在抓取 AIBASE 新闻源...
✅ AIBASE: 成功抓取 10 条新闻

✅ 总计抓取 10 条新闻
   来源分布: aibase=10
```

### 多新闻源模式

```
================================================================================
🚀 自动化内容生成流水线启动
================================================================================
新闻源: aibase, qbitai
每个源抓取新闻数: 10
生成文章数: 2
搜索深度: quick
输出目录: posts
================================================================================

📰 正在抓取 AIBASE 新闻源...
✅ AIBASE: 成功抓取 10 条新闻

📰 正在抓取 QBITAI 新闻源...
✅ QBITAI: 成功抓取 10 条新闻

✅ 总计抓取 20 条新闻
   来源分布: aibase=10, qbitai=10
```

---

## 测试单个爬虫

### 测试 AIBase 爬虫

```bash
python generate/aibase_crawler.py
```

**输出示例**：
```
======================================================================
🚀 开始抓取 AIBase 最新 AI 新闻 TOP10
======================================================================

📡 正在请求: https://www.aibase.com/zh/news
✓ 找到 10 篇新闻

  [1] ✓ 中文图像编辑迎来新王！UniWorld-V2发布...
  [2] ✓ 抖音启动AI仿冒专项治理...
  ...

======================================================================
✅ 成功抓取 10 条新闻
======================================================================
```

### 测试量子位爬虫

```bash
python generate/qbitai_crawler.py
```

---

## 新闻源数据结构

### AIBase 新闻项

```python
{
    'title': '文章标题',
    'url': '文章链接',
    'summary': '文章摘要',
    'time': '发布时间',
    'source': '来源（如 AIbase）',
    'image_url': '封面图片 URL',
    'news_source': 'aibase',  # 新闻源标识
    'crawled_at': '2025-11-07T10:30:00'
}
```

### 量子位新闻项

```python
{
    'title': '文章标题',
    'url': '文章链接',
    'summary': '文章摘要',
    'author': '作者',
    'time': '发布时间',
    'tags': ['标签1', '标签2'],
    'image_url': '封面图片 URL',
    'news_source': 'qbitai',  # 新闻源标识
    'crawled_at': '2025-11-07T10:30:00'
}
```

---

## 添加新的新闻源

### 1. 创建爬虫文件

在 `generate/` 目录下创建新的爬虫文件，例如 `newsource_crawler.py`：

```python
class NewSourceCrawler:
    """新闻源爬虫"""
    
    def __init__(self, timeout: int = 10):
        """初始化爬虫"""
        pass
    
    def fetch_top_news(self, limit: int = 10) -> List[Dict[str, str]]:
        """
        抓取最新新闻
        
        Returns:
            新闻列表，每条新闻至少包含：
            {
                'title': 标题,
                'url': 链接,
                'summary': 摘要 (可选),
                'time': 发布时间 (可选),
                'news_source': 'newsource'  # 必需！
            }
        """
        # 实现抓取逻辑
        pass
```

### 2. 注册到流水线

编辑 `generate/auto_content_pipeline.py`：

```python
from generate.newsource_crawler import NewSourceCrawler

class AutoContentPipeline:
    # 新闻源映射
    CRAWLER_MAP = {
        'qbitai': QbitAICrawler,
        'aibase': AIBaseCrawler,
        'newsource': NewSourceCrawler  # 添加新源
    }
```

### 3. 更新配置文件

```yaml
# config/common.yaml
news_sources: aibase,qbitai,newsource
```

---

## 常见问题

### Q1: 如何只使用 AIBase 新闻源？

**A**: 修改 `config/common.yaml`：
```yaml
news_sources: aibase
```

### Q2: 多新闻源时，新闻是如何合并的？

**A**: 所有新闻源的新闻会按抓取顺序合并成一个列表，然后从头开始选择生成文章。

例如：
- AIBase 抓取 10 条
- 量子位抓取 10 条
- 合并为 20 条（AIBase 的 10 条 + 量子位的 10 条）
- 生成文章时从前 N 条选择

### Q3: 如何查看每条新闻来自哪个新闻源？

**A**: 查看中间数据文件 `data/generated/01_crawled_news.json`，每条新闻都有 `news_source` 字段。

### Q4: 某个新闻源抓取失败怎么办？

**A**: 流水线会继续使用其他成功的新闻源。如果所有新闻源都失败，流水线会终止。

### Q5: 可以为不同新闻源设置不同的抓取数量吗？

**A**: 目前所有新闻源使用相同的 `--news-limit`。如需不同数量，建议单独运行爬虫。

---

## 最佳实践

### 1. 推荐配置

**日常使用**：
```yaml
# 使用 AIBase（更新快，内容多样）
news_sources: aibase
```

**深度覆盖**：
```yaml
# 同时使用两个源（内容更全面）
news_sources: aibase,qbitai
```

### 2. 性能优化

```bash
# 多源模式下，减少每个源的抓取数量
python generate/auto_content_pipeline.py \
    --news-sources aibase,qbitai \
    --news-limit 5 \
    --article-limit 2
```

### 3. 调试单个新闻源

```bash
# 先测试单个爬虫是否正常
python generate/aibase_crawler.py

# 再集成到流水线
python generate/auto_content_pipeline.py --news-sources aibase
```

---

## 附录：AIBase 页面元素说明

AIBase 新闻页面结构：

```html
<!-- 新闻列表容器 -->
<div class="grid grid-cols-1 md:grid-cols-1 gap-6 w-full">
    <!-- 单个新闻项 -->
    <a class="flex group justify-between..." href="/zh/news/22611">
        <div class="flex flex-col flex-1...">
            <!-- 时间和来源 -->
            <div class="text-sm text-gray-400...">
                <span>11 分钟前</span>
                <span>AIbase</span>
            </div>
            <!-- 标题 -->
            <h3 class="line-clamp-2...">新闻标题</h3>
            <!-- 摘要 -->
            <div class="text-[15px] line-clamp-2 text-surface-500">
                新闻摘要内容...
            </div>
        </div>
        <!-- 封面图 -->
        <img src="..." />
    </a>
</div>
```

爬虫通过 BeautifulSoup 解析这些元素提取新闻信息。

---

## 更新日志

- **2025-11-07**: 添加 AIBase 新闻源，设为默认
- **2025-11-07**: 支持多新闻源配置（逗号分隔）
- **2025-11-07**: 添加命令行 `--news-sources` 参数
- **2025-11-07**: 更新统计报告，显示新闻源分布
